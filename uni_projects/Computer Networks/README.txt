Implementation of the microTCP packet structure, the 3-way handshake and finalize the microTCP connection.
Implementation of a TCP file transfer tool which will be used at the end to compare the performance 
of the implementation against the full TCP implementation. 
Further implementation of the basic TCP function the
which includes the mechanisms for acknowledgements, retransmissions, error checking, TCP windowing, Congestion control and Slow start.